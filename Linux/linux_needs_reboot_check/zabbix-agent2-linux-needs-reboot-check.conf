# NeedsRebootCheck (Linux) - Zabbix Agent 2 Go plugin
# Key: system.needs_reboot

# Plugin mode (recommended):
Plugins.NeedsRebootCheck.System.Path=/usr/sbin/zabbix-agent2-plugin/zabbix-agent2-needs-reboot-check

# If you prefer UserParameter style, the binary supports standalone mode:
# UserParameter=system.needs_reboot,/usr/sbin/zabbix-agent2-plugin/zabbix-agent2-needs-reboot-check --standalone
